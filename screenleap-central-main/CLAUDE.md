# SignCMS Project Instructions

## Git workflow
- After every `git commit`, immediately run `git push origin master` in the same session.
- Never leave commits unpushed — always push in the same turn as the commit.
